# ============================================================================
# Glass — Backend (FastAPI)
# Dependencias alineadas con los ADR (ver docs/adr/adr.md):
#   ADR-003  Stack: FastAPI + PostgreSQL/PostGIS
#   ADR-006  PostgreSQL + PostGIS
#   ADR-007  Caché con Redis
#   ADR-002  ETL batch con APScheduler
#   ADR-001  Extracción de partidas vía Gemini API
#   ADR-009  Configuración por variables de entorno
# ============================================================================

# --- Núcleo web / ASGI ---
fastapi==0.115.*
uvicorn[standard]==0.32.*

# --- Configuración (12-factor, ADR-009) ---
pydantic==2.9.*
pydantic-settings==2.6.*
python-dotenv==1.0.*
email-validator==2.2.*

# --- Tests ---
pytest==8.3.*

# --- Base de datos (ADR-006: PostgreSQL + PostGIS) ---
SQLAlchemy==2.0.*
GeoAlchemy2==0.15.*
psycopg2-binary==2.9.*
alembic==1.13.*

# --- Caché (ADR-007: Redis) ---
redis==5.2.*

# --- Jobs / ETL (ADR-002: APScheduler) ---
APScheduler==3.10.*

# --- Cliente HTTP y scraping de fuentes externas (SEACE, SUNAT, INEI, JNE) ---
httpx==0.27.*
beautifulsoup4==4.12.*
openpyxl==3.1.*
pandas==2.2.*

# --- Extracción de PDF vía IA (ADR-001) ---
google-generativeai==0.8.*

# --- Autenticación (RNF-08: hashing + JWT) ---
passlib[bcrypt]==1.7.*
python-jose[cryptography]==3.3.*
